# Handoff — <Название прототипа>

Заметки для разработки. Заполнить перед передачей.

## Статус фаз

Фазы воркфлоу (`memory-bank/conventions.md`); чекбокс — только после реального прохождения гейта.

- [ ] Вход зафиксирован (Figma-состояния + Motion-node — см. ниже)
- [ ] Статика всех состояний заапрувлена пользователем — гейт `proto-static`
- [ ] Анимация готова — `proto-animate`
- [ ] Ревью `/review-animations` — вердикт Approve
- [ ] Передача выполнена (DoD, деплой/экспорт) — `proto-handoff`

## Источник / вход

Минимальный Figma-вход (зафиксировать ДО сборки, см. `memory-bank/conventions.md`):

- Состояние `<имя>`: Figma node-id `<...>`
- Состояние `<имя>`: Figma node-id `<...>`
- Motion-node (easing/длительности/пружины): `<...>`

## Что это

Короткое описание флоу и цели прототипа. Таргет: `mobile-app | desktop-web | adaptive`.

## Экраны и навигация

- Список экранов (`nav`-идентификаторы) и переходов между ними.
- Что использовано: `View` (push), `Epic` (табы), `ModalRoot` (шторки), `Root` (стеки View).

## Анимации

| Переход / эффект | Источник | Длительность / easing | Примечание |
| ---------------- | -------- | --------------------- | ---------- |
| Push между панелями | VKUI `View` (дефолт) | дефолт VKUI | не трогали |
| <кастомный эффект> | Framer Motion | <ms / spring params> | см. `src/components/...` |

- Что является дефолтом VKUI (переносится как есть), а что — кастом (нужно воспроизвести).
- Reduced-motion: как обрабатывается (`transitionMotionEnabled`, `useReducedMotion`).

## Маппинг компонентов

- Какие VKUI-компоненты использованы (и их аналоги в дизайн-системе/Figma, если есть).

## Мок-данные и упрощения

- Где данные замоканы, какой стейт/логика НЕ реализованы и должны быть достроены.
