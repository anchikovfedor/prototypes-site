import { PrototypeRoot } from '@proto/kit';
import { AppContent } from './AppContent';

export function App() {
  return (
    <PrototypeRoot target="adaptive">
      <AppContent />
    </PrototypeRoot>
  );
}
